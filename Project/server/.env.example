# RentalWeb Backend Environment Variables
# 이 파일을 .env로 복사한 뒤 실제 값을 입력하세요.

# 1. Database Configuration (Supabase or Local Postgres)
# 형식: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://postgres:your_password@localhost:5432/rentalweb?schema=public"

# 2. JWT Configuration
# 인증 토큰 생성에 사용될 비밀키입니다. (다른 컴퓨터에서도 동일하게 맞춰야 기존 토큰이 유효함)
JWT_SECRET="your_super_secret_key_here"

# 3. Server Configuration
PORT=3000
NODE_ENV=development

# 4. Supabase Storage Configuration
SUPABASE_URL="https://your-project-ref.supabase.co"
SUPABASE_KEY="your-service-role-key"
SUPABASE_BUCKET_NAME="rental-web"

# 5. SMS Service Configuration (Solapi/CoolSMS)
SOLAPI_API_KEY="your_api_key"
SOLAPI_API_SECRET="your_api_secret"
SOLAPI_SENDER_NUMBER="01012345678"
